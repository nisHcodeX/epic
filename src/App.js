import './App.css';

function App({attemptLogin, authReducer}) {
  return (
    <div style={{textAlign : 'center'}}>
        <h1>Welcome</h1>
        <button>Login</button>
    </div>
  );
}

export default App;
